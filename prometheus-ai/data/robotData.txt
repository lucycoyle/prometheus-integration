charge(probability>0); 100
charge(probability>0); 100; charge(probability>50) -> @chargeNow(willDie); 100
frontSensor(probability>0); 100; leftSensor(probability>0); 100; rightSensor(probability>0); 100; backSensor(probability>0); 100
frontSensor(probability>0); 100; backSensor(probability>0); 100; frontSensor(probability>0) backSensor(probability>0) -> @move(right); 100
backSensor(probability>0); 100; leftSensor(probability>0); 100; backSensor(probability>0) leftSensor(probability>0) -> @move(front); 100
backSensor(probability>0); 100; rightSensor(probability>0); 100; backSensor(probability>0) leftSensor(probability>0) -> @move(left); 100
leftSensor(probability>0); 100; frontSensor(probability>0); 100; leftSensor(probability>0) frontSensor(probability>0) -> @move(front); 100
charge(probability>0) -> @chargeNow(willDie); 100
